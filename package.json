{
  "name": "testelysee",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "predeploy": "pnpm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.2.0"
  },
  "dependencies": {
    "gh-pages": "^6.3.0"
  }
}
